import React from 'react';
import { useInView } from 'react-intersection-observer';
import Header from "../components/Header"; // Your existing Header component

export default function Page() {
  // Hooks for each section
  const { ref: homeRef, inView: homeInView } = useInView({ triggerOnce: true, threshold: 0.1 });
  const { ref: aboutRef, inView: aboutInView } = useInView({ triggerOnce: true, threshold: 0.1 });
  const { ref: joinRef, inView: joinInView } = useInView({ triggerOnce: true, threshold: 0.1 });

  return (
    <>
      <Header />

      {/* Home Section */}
      <section id="home" ref={homeRef} className={`transition-opacity duration-1000 ${homeInView ? 'opacity-100' : 'opacity-0'}`}>
        <div className="banner-text">
          <h1> Hindu YUVA at UCR </h1>
          <h2>A platform to preserve, practice, promote, and protect Dharma at UCR.</h2>
          <a target="_blank" href="https://highlanderlink.ucr.edu/organization/hinduyuvaucr">
            <button className="mt-[8%] w-60 text-white hover:text-black font-bold py-3 px-9 rounded-full hover:bg-blue-100 transition-transform">
              Join Now
            </button>
          </a>
        </div>
      </section>

      {/* About Section */}
      <section id="about" ref={aboutRef} className={`transition-opacity duration-1000 ${aboutInView ? 'opacity-100' : 'opacity-0'}`}>
        <div className="about-content">
          <h2>Who Are We?</h2>
          <p>Hindu YUVA aims to provide a platform to preserve, practice, promote, and protect Hindu Dharma...</p>
        </div>
      </section>

      {/* Join Section */}
      <section id="join" ref={joinRef} className={`transition-opacity duration-1000 ${joinInView ? 'opacity-100' : 'opacity-0'}`}>
        <div className="join-content">
          <h2>Join Our Community</h2>
          <p>Follow us on social media to stay updated...</p>
        </div>
      </section>
    </>
  );
}
